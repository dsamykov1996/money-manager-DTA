# money-manager-DTA
## 🛒 Для чого створена ця програма?

Ця програма — це простий **менеджер списку покупок**, який працює у консолі. Вона допомагає користувачу керувати своїми покупками за допомогою зручного меню.

---

### **1. Додавання товарів**
Користувач може додати новий товар, вказавши:
- назву,
- кількість,
- ціну за одиницю.

Програма зберігає це у вигляді словника.

---

### **2. Перегляд списку покупок**
Функція `show_list()` виводить усі додані товари у структурованому вигляді.  
Зручно для перевірки того, що вже є у списку.

---

### **3. Підрахунок загальної суми**
Функція `count_total()` обчислює загальну вартість усіх покупок.  
Це дозволяє контролювати бюджет.

---

### **4. Збереження списку у файл**
Функція `save_to_file()` записує весь список товарів у файл `text.txt`.  
Дані можна зберегти, щоб не втратити після закриття програми.

---

### **5. Завантаження списку з файлу**
Функція `load_from_file()` читає файл і відновлює список покупок.  
Це дозволяє продовжити роботу зі списком пізніше.

---

### **6. Зручне меню керування**
У функції `main()` є меню, де користувач може вибрати одну з доступних дій (1–6).  
Це робить програму схожою на простий консольний застосунок.

---

## 🎯 Підсумок
Ця програма створена для практики:
- роботи з функціями,
- введення й виведення даних,
- обробки помилок,
- роботи зі списками та словниками,
- читання та запису файлів.

Вона дозволяє зручно додавати, переглядати, рахувати, зберігати та завантажувати список покупок.


